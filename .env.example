# Database configuration
# Option 1: Use DATABASE_URL (preferred for Render)
DATABASE_URL=postgres://username:password@host:port/database_name

# Option 2: Use individual components (for local development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=calespero
DB_SSL_MODE=require  # Use 'disable' for local development, 'require' for Render

# JWT configuration
JWT_SECRET_KEY=your-secret-key-here 